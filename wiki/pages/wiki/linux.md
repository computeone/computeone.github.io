
```
 task_struct、fork、clone、exec、nsproxy、kernel
thread、schedule 
```



```
 UMA、NUMA、zone、page、伙伴系统、slab分配器、slub分配器、

```



```
 mm_struct、vm_area_struct、区域操作、mmap、缺页处理 
```



```

信号量、原子操作、自旋锁、读写锁、RCU、大内核锁、互斥量、System
V(信号量、消息队列、共享内存)、信号、 管道、套接字 
```



```

moudlue、加载模块、卸载模块、模块依赖、热插拔、POSIX标准、软件中断、硬件中断、中断处理、
tasklet、等待队列、完成量、定时器 
```



```

设备文件、字符设备、块设备、设备号、设备注册、cdev、request、BIO、I/O调度、I/O内存、
总线系统、设备驱动、PCI总线、设备管理 
```



```

super_block、inode、files_struct、文件操作、vfs命名空间、dentry、vfsmount、
软连接、硬链接、权限检查、ext文件系统、无持久存储文件系统、proc文件系统、
proc_dir_entry、proc inode、proc mount、简单文件系统、sysfs 
```



```

网络命名空间、sk_buff、net_device、网络设备注册、接受分组、netfilter、socket、
proto_ops、sock、发送数据、接受数据、netlink 
```

